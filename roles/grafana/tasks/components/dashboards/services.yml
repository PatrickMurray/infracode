---
- name: Include components
  include_tasks:
    file: "services/{{ service }}.yml"
  loop_control:
    loop_var: service
  loop:
    - grafana
    - influxdb
    - telegraf

    - dns
    - minecraft
    - bitwarden
    
    # ZZZ-Trading
    - gitlab
    - docker_registry
